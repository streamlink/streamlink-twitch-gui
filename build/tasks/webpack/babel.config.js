// global babel{,-loader} config which will be merged by each specific config
module.exports = {
	// babel-loader config
	// resolves to `node_modules/.cache/babel-loader`
	cacheDirectory: true,

	// babel config
	parserOpts: {},
	presets: [],
	plugins: []
};
