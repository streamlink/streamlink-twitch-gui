default:
    - "build"
test:
    - "jshint:test"
    - "clean:tmp"
    - "copy:test"
    - "webpack:test"
    - "connect:test"
    - "qunit"
build:
    - "build:dev"
build:dev:
    - "jshint:tasks"
    - "jshint:app"
    - "clean:tmp"
    - "copy:build"
    - "svgmin"
    - "webpack:dev"
build:prod:
    - "jshint:tasks"
    - "jshint:app"
    - "test"
    - "clean:tmp"
    - "copy:build"
    - "svgmin"
    - "webpack:prod"
