default:
    - "dev"
dev:
    - "build"
    - "concurrent:dev_watchers"
test:
    - "jshint:test"
    - "connect:test"
    - "qunit"
build:
    - "jshint:app"
    - "clean:tmp"
    - "copy:build"
    - "copy:flags"
    - "metadata"
    - "less:dev"
    - "requirejs:dev"
buildrelease:
    - "jshint:app"
    - "test"
    - "clean:tmp"
    - "copy:build"
    - "copy:flags"
    - "metadata"
    - "less:release"
    - "requirejs:release"
    - "uglify"
