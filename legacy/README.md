# Legacy application (NW.js + Ember)

This directory contains the pre-rewrite Streamlink Twitch GUI (v2.5.x era).

It is kept as a reference for feature parity while the Tauri 2 + React app is built at the repository root.

Do not expect `legacy/` to receive new features.
