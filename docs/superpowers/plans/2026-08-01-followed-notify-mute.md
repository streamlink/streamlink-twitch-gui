# Followed Notify Mute Implementation Plan

> **For agentic workers:** Use executing-plans. Checkbox steps for tracking.

**Goal:** Per-channel mute list for followed-live desktop notifications.

**Spec:** `docs/superpowers/specs/2026-08-01-followed-notify-mute-design.md`

## Tasks

1. Settings schema 14 + `mutedFollowed` migrate/tests
2. Filter in `DesktopChrome`; channel + settings UI; locales; changelog
3. Verify + commit
