twitch:
    channel:
        followers: |
            {count, plural,
                one {One person is following}
                other {# people are following}
            }
        views: |
            {count, plural,
                one {One channel view}
                other {# channel views}
            }
    stream:
        created-at:
            # new Moment(stream.created_at).format(...)
            less-than-24h: "[Online since] LTS"
            # new Moment(stream.created_at).format(...)
            more-than-24h: "[Online since] llll"
        viewers: |
            {count, plural,
                one {One person is watching}
                other {# people are watching}
            }
