twitch:
    user:
        view_count: |
            {count, plural,
                one {# просмотр}
                few {# просмотра}
                many {# просмотров}
                other {Просмотров: #}
            }
    stream:
        started-at:
            less-than-24h: "Запущен в {started_at, time, medium}"
            more-than-24h: "Запущен в {started_at, time, long}"
        viewer_count: |
            {count, plural,
                one {# зритель}
                few {# зрителя}
                many {# зрителей}
                other {Зрителей: #}
            }
    search-channel:
        started-at:
            offline: "не в сети"
            less-than-24h: "Запущен в {started_at, time, medium}"
            more-than-24h: "Запущен в {started_at, time, long}"
