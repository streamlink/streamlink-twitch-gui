twitch:
    channel:
        followers: |
            {count, plural,
                one {# человек подписан}
                few {# человека подписано}
                many {# человек подписано}
                other {Подписчиков: #}
            }
        views: |
            {count, plural,
                one {# просмотр}
                few {# просмотра}
                many {# просмотров}
                other {Просмотров: #}
            }
    stream:
        created-at:
            # new Moment(stream.created_at).format(...)
            less-than-24h: "[Запущен в] LTS"
            # new Moment(stream.created_at).format(...)
            more-than-24h: "[Запущен в] llll"
        viewers: |
            {count, plural,
                one {# зритель}
                few {# зрителя}
                many {# зрителей}
                other {Зрителей: #}
            }
