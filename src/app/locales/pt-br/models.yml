twitch:
    channel:
        followers: |
            {count, plural,
                one {Uma pessoa está seguindo}
                other {# pessoas estão seguindo}
            }
        views: |
            {count, plural,
                one {Apenas uma visualização}
                other {# visualizações}
            }
    stream:
        created-at:
            # new Moment(stream.created_at).format(...)
            less-than-24h: "[Online desde] LTS"
            # new Moment(stream.created_at).format(...)
            more-than-24h: "[Online desde] llll"
        viewers: |
            {count, plural,
                one {Uma pessoa está assistindo}
                other {# estão assistindo}
            }
