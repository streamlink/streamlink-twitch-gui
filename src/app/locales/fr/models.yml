twitch:
    channel:
        followers: |
            {count, plural,
                one {Une personne suit cette chaîne}
                other {# personnes suivent cette chaîne}
            }
        views: |
            {count, plural,
                one {Une vue de la chaîne}
                other {# vues de la chaîne}
            }
    stream:
        created-at:
            # new Moment(stream.created_at).format(...)
            less-than-24h: "[Online since] LTS"
            # new Moment(stream.created_at).format(...)
            more-than-24h: "[Online since] llll"
        viewers: |
            {count, plural,
                one {Une personne est en train de regarder}
                other {# personnes sont en train de regarder}
            }
