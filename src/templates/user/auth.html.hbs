<div class="bigicon"><i class="fa fa-user"></i></div>
{{#form-button action="signin" class="btn-signin" icon="fa-sign-in" disabled=auth_lock}}Sign in{{/form-button}}
<footer>
{{#if settings.advanced}}
{{#if view.showTokenForm}}
<div class="input-group token-form">
{{input type="text" action="inputAction" targetObject=view class="form-control" value=view.token placeholder="Insert access token from another device"}}
<span class="input-group-btn">
{{form-button action="signinToken" targetObject=view class=view.tokenBtnClass icon=view.tokenBtnIcon iconanim=true}}
</span>
</div>
{{else}}
{{#form-button action="showTokenForm" targetObject=view class="btn-token" icon="fa-key" disabled=auth_win_lock}}Add access token{{/form-button}}
{{/if}}
{{/if}}
{{#if auth_failure}}
<p class="fail">Authentication failed. Please try again.</p>
{{/if}}
</footer>