{{#each _content as |button|}}
{{radio-button id=button._id name=buttonName value=button.value selection=value disabled=button.disabled class="default"}}
<label for="{{button._id}}" class="{{if icon 'btn-with-icon'}}">{{#if icon}}<i class="fa fa-check"></i>{{/if}}{{button.label}}</label>
{{/each}}