<div>
<header>{{#if head}}{{unbound head}}{{else}}Please confirm{{/if}}</header>
<section class="content">{{#if body}}{{unbound body}}{{else}}Are you sure?{{/if}}</section>
<footer>
{{#each button in buttons}}
<button type="button" class="btn btn-with-icon {{unbound button.button}}" {{action "close" button.action}}><i class="fa {{unbound button.icon}}"></i> {{unbound button.value}}</button>
{{else}}
<button type="button" class="btn btn-with-icon btn-success" {{action "close"}}><i class="fa fa-check"></i> OK</button>
{{/each}}
</footer>
</div>